# laser-mace-testing (private)

This package acts as a placeholder for the private `laser-mace-testing` repository.

To populate it with the real sources, authenticate with GitHub and run:

```bash
npm run fetch:laser-mace-testing
```

or manually clone the repository into this directory.
